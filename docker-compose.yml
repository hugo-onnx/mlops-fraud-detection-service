version: "3.9"

services:
  fraud-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fraud-api
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Optional volume mounts for local development:
    # volumes:
    #   - ./models:/app/models
    #   - ./data:/app/data
